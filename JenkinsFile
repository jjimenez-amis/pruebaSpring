pipeline {
	agent any

	environment{
	DOCKER_BUILDER_USERNAME_HCARGA = "${params.DOCKER_BUILDER_USERNAME_HCARGA}"
	}
	stages{
		stage("Run"){
 			steps{
				sh 'chmod 777 ./gradlew'
				sh './gradlew -PfirstParam=${DOCKER_BUILDER_USERNAME_HCARGA} pruebaParametro'
			}
	        }
	}

}
